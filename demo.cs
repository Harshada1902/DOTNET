using System;

public class TriangleArea
{
    public static void Main(string[] args)
    {
        Console.WriteLine("Enter the base of the traingle:");
        double baseofTraingle=convert.ToDouble(console.ReadLine());

        console.WriteLine("Enter the height of the traingle:");
        double.height=convert.ToDouble(console.ReadLine());
    
        double area=(baselength*height)/2;
        console.WriteLine("The area of the traingle is:"+area);
    }
}